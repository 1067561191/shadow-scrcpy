# This file must be sourced from the release scripts directory
WORK_DIR="$PWD/work"
OUTPUT_DIR="$PWD/output"

VERSION="${VERSION:-$(git describe --tags --always)}"
# Sanitize version string: replace / with - for valid file paths
VERSION="${VERSION//\//-}"
